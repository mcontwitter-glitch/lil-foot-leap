# LIL Foot Leap

LIL Foot Leap is a small pond/swamp pad-jump game. Guide the character across lily pads and enjoy the playful swamp scene.

## Run it

No build step is required. Open `index.html` directly in a browser, or serve the project locally:

```bash
python -m http.server
```

Then visit http://localhost:8000.
